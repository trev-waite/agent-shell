{
  "name": "@relay/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "bun --env-file=../../.env run --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@relay/coordination": "workspace:*",
    "@relay/dispatch": "workspace:*",
    "@relay/providers": "workspace:*",
    "@relay/pubsub": "workspace:*",
    "@relay/runtime": "workspace:*",
    "@relay/storage": "workspace:*",
    "@relay/tool-registry": "workspace:*",
    "@relay/tools": "workspace:*",
    "@relay/types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.13.0",
    "bun-types": "1.3.14",
    "typescript": "^6.0.3"
  }
}
