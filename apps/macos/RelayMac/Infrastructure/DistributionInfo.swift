import Foundation

/// Phase 3 scaffold — code signing and distribution hooks.
public enum DistributionInfo {
    public static let bundleIdentifier = "com.relay.mac"
    public static let appName = "Relay"
    public static let minimumOSVersion = "26.0"
}
